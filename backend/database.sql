CREATE DATABASE IF NOT EXISTS icytales;
USE icytales;

-- Spring Boot/JPA creates the tables automatically with spring.jpa.hibernate.ddl-auto=update.
-- Run this file first if you prefer to create the database manually.
